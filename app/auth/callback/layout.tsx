import type { Metadata } from "next";

export const metadata: Metadata = { title: "Memeriksa Tautan" };

export default function AuthCallbackLayout({ children }: { children: React.ReactNode }) {
  return children;
}
